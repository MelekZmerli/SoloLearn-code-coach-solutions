#include <iostream>
using namespace std;

int main(){
  int tickets,gun;
  scanf("%d",&tickets);
  scanf("%d",&gun);
  tickets=/12;
  if(tickets>=gun){
    printf("Buy it!");
  }
  else
    printf("Try again");
  return 0;
}
